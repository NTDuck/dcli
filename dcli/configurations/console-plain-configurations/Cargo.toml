[package]
name = "console-plain-configurations"
version = "0.1.0"
edition.workspace = true

[dependencies]
serde = { workspace = true }

models = { path = "../../core/use-cases/models" }

rest-api-adapters = { path = "../../infrastructures/adapters/rest-api-adapters", features = ["client"] }

clap = "4.5"

[lints]
workspace = true

[package]
name = "interface-adapters"
version = "0.1.0"
edition = "2021"

[dependencies]
layout = { path = "../../../libs/layout" }
domain = { path = "../../core/domain" }
use-cases = { path = "../../core/use-cases" }

chrono = "0.4.39"

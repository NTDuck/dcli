[package]
name = "rest-api-adapters"
version = "0.1.0"
edition.workspace = true

[dependencies]
axiom = { workspace = true }
serde = { workspace = true }

use-cases = { path = "../../../core/use-cases", features = ["boundaries"] }

axum = "0.8"
# paste = "1.0"
serde_qs = "0.14"
ureq = { version = "3.0", features = ["json"] }

[lints]
workspace = true

[features]
client = []
server = []

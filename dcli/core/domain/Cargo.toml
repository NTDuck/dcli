[package]
name = "domain"
version = "0.1.0"
edition.workspace = true

[dependencies]
axiom = { path = "../../../libs/axiom" }

serde = { version = "1.0", features = ["derive"] }

[lints]
workspace = true

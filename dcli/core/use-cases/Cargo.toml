[package]
name = "use-cases"
version = "0.1.0"
edition.workspace = true

[features]
default = ["boundaries", "interactors", "gateways"]
boundaries = []
interactors = []
gateways = []

[dependencies]
axiom = { workspace = true }
serde = { workspace = true }

domain = { path = "../domain" }

[lints]
workspace = true

[package]
name = "use-cases"
version = "0.1.0"
edition.workspace = true

[dependencies]
axiom = { workspace = true }
serde = { workspace = true }

domain = { path = "../domain" }

[features]
default = ["boundaries", "gateways", "interactors"]
boundaries = []
gateways = []
interactors = []

[lints]
workspace = true

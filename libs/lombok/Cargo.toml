[package]
name = "lombok"
version = "0.1.0"
edition.workspace = true

[dependencies]
modules = { path = "../modules" }
lombok-derive = { path = "../lombok-derive" }

serde = "1.0"

[lints]
workspace = true

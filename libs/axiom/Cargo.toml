[package]
name = "axiom"
version = "0.1.0"
edition.workspace = true

[dependencies]
derive = { package = "axiom-derive", path = "../axiom-derive" }

serde = { workspace = true }

[lints]
workspace = true
